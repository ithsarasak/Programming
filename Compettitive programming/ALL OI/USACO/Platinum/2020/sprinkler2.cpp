#include <bits/stdc++.h>

using namespace std;

int n;

long long solve( int x, int y, int px, int py ) {
    if( dp[x][y] != -1 ) return dp[x][y];
    long long &now = dp[x][y];
    now = 0;
    for( int i = y ; i <= n ; i++ ) {
        now 
    }

}

int main()
{
    scanf("%d",&n);
    for( int i = 1 ; i <= n ; i++ ) scanf("%s",&s[i][1]);
    solve( 1, 1 );
    return 0;
}