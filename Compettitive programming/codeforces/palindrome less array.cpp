#include <bits/stdc++.h>

using namespace std;

int n;
long long k;
int main()
{
	scanf("%d %lld",&n,&k);
	for( int i = 1 ; i <= n ; i++ ) scanf("%d",&num[i]);
	if( num[1] == -1 ) st = 1, en = k
	else st = num[1], en = num[1];
	if( num[2] == -1 ) st2 = 1, en2 = k;
	else st2 = num[2], en[
	for( int i = 1 ; i <= n ; i++ ) {
		if( 
	}
	return 0;
}